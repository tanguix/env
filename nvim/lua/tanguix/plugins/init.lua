
-- load the basic plugins that doesn't need much configuration 
return {
  "nvim-lua/plenary.nvim",            -- I don't know, just include this, because many other plugins use it
  "christoomey/vim-tmux-navigator",   -- bind neovim default window navigation with tmux keybind (but I never use it)
}
